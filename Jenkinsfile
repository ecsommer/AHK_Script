pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python3 -V'
                sh 'sudo apt-get install -y python3-pip'
            }
        }
    }
}
